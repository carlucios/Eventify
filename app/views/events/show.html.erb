<div id="event" data-controller="event">
  <h1><%= @event.title %></h1>
  <p><%= @event.description %></p>

  <%= turbo_frame_tag "notifications" do %>
    <!-- Aqui pode renderizar notificações recebidas -->
  <% end %>
</div>
